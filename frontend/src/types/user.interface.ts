export type IUser = {
    id: number;
    username: string;
    hidden: boolean;
};

export type IStreamOption = {
    token: string;
    name?: string;
    emote_id?: string;
    public: boolean
};

